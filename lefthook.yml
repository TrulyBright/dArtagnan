pre-push:
  commands:
    packages-audit:
      run: pnpm audit

pre-commit:
  parallel: true
  commands:
    check-types:
      run: turbo check-types
    format-and-lint:
      run: turbo format-lint:fix
    test:
      run: turbo test